
name: Deploy to Remote react-app-Server in OCI

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout code
      uses: actions/checkout@v2


    - name: Set up server.key file
      env: 
        SERVER_KEY: ${{secrets.SERVER_KEY}}
      run:
        echo "$SERVER_KEY" > /home/runner/work/moviestreamingClient/moviestreamingClient/server.key
        chmod 600 /home/runner/work/moviestreamingClient/moviestreamingClient/server.key



    - name: Set up server.crt file
      env: 
        SERVER_CERTIFICATE: ${{secrets.SERVER_CERTIFICATE}}
      run:
          echo "$SERVER_CERTIFICATE" > /home/runner/work/moviestreamingClient/moviestreamingClient/server.crt
          chmod 600 /home/runner/work/moviestreamingClient/moviestreamingClient/server.crt

    
    - name: setting .env file
      env: 
        API_SERVER: ${{secrets.API_SERVER}}
        API_SERVER_PORT: ${{secrets.API_SERVER_PORT}}
      run: |
            echo "API_SERVER=$API_SERVER" > /home/runner/work/moviestreamingClient/.env
            echo "API_SERVER_PORT=$API_SERVER_PORT" >> /home/runner/work/moviestreamingClient/.env

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18 

    - name: Install dependencies
      run: npm install

    - name: Build the React app
      run: npm run build


    - name: Set up SSH key
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      run: |
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa



    - name: Add remote server to known hosts
      run: |
        ssh-keyscan -v 155.248.245.222 >> ~/.ssh/known_hosts



    - name: Sync files to remote server
      run: |
        rsync -avz --delete-after ./dist/ opc@155.248.245.222:/home/opc/moviestreamingClient